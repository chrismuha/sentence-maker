// Preload keeps the renderer isolated from Node APIs. Currently unused but
// reserved for future enhancements that need a safe bridge.
window.addEventListener("DOMContentLoaded", () => {
  // placeholder: DOM is ready
});
